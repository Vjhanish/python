class Solution(object):
    def sumZero(self, n):
        """
        :type n: int
        :rtype: List[int]
        """
        res=[i for i in range(1,n)]
        res.append(-(sum(res)))

        return res
