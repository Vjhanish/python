class Solution(object):
    def repeatedNTimes(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        a = set()
        for num in nums:
            if num in a:
                return num
            a.add(num)
