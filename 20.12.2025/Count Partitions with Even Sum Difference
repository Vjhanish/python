class Solution(object):
    def countPartitions(self, nums):
        count = 0
        for i in range(1,len(nums)):
            a = nums[:i]
            b = nums[i:]
            if abs(sum(b) - sum(a))%2 == 0:
                count += 1
        return count
        
